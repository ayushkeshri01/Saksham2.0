# Easebuzz Wire payout credentials (from your Easebuzz dashboard)
EASEBUZZ_WIRE_KEY=1Z1234567X
EASEBUZZ_WIRE_SALT=your_wire_salt_here
EASEBUZZ_VIRTUAL_ACCOUNT=100001000000xxxxx
# Optional override for the API base (defaults to wire.easebuzz.in)
EASEBUZZ_API_BASE=https://wire.easebuzz.in/api/v1

# PostgreSQL connection (defaults shown)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=partlog_db

# Optional: set to 'true' to wipe condenser/compressor logs on server start (dev only)
RESET_ENTRIES_ON_START=false

# Used to validate portal JWT tokens
JWT_SECRET=change_this_to_a_long_random_string

# MSG91 OTP credentials
MSG91_AUTHKEY=567821TECBH4N2ju6a9bada7P1
MSG91_WIDGET_ID=366965655632323634373032
